export function mergeSort(arr: any[]): any[] {
    let result = [...arr];

    return result;
}

export function merge(A: any[], B: any[]) {
    let result: any[] = [];
    let a = 0;
    let b = 0;
    while (a < A.length && b < A.length) {
        
    }
    return result;
}